-- MySQL Script generated by MySQL Workbench
-- Tue Sep 15 11:09:10 2026
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema dojify_1
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema dojify_1
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `dojify_1` DEFAULT CHARACTER SET utf8mb4 ;
USE `dojify_1` ;

-- -----------------------------------------------------
-- Table `dojify_1`.`academia`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`academia` (
  `id_academia` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da academia no sistema.',
  `nome` VARCHAR(100) NOT NULL COMMENT 'Nome comercial da academia exibido na interface.',
  `documento` VARCHAR(14) NOT NULL COMMENT 'Número do CNPJ ou CPF (do responsável pela academia).',
  `endereco` VARCHAR(100) NOT NULL COMMENT 'Endereço da academia.\n',
  `telefone` VARCHAR(20) NOT NULL COMMENT 'Número de contato principal.\n',
  `email` VARCHAR(100) NOT NULL COMMENT 'E-mail institucional da academia.',
  `data_cadastro` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT 'Data e hora de inclusão da academia.\n',
  PRIMARY KEY (`id_academia`),
  UNIQUE INDEX `cnpj` (`documento` ASC) ,
  UNIQUE INDEX `email` (`email` ASC) )
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;


-- -----------------------------------------------------
-- Table `dojify_1`.`perfil`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`perfil` (
  `id_perfil` INT NOT NULL AUTO_INCREMENT COMMENT 'Identificador único do perfil de acesso.',
  `nome` VARCHAR(50) NOT NULL COMMENT 'Nome do papel (ADMINISTRADOR, GERENTE, PROFESSOR, ALUNO).',
  PRIMARY KEY (`id_perfil`),
  UNIQUE INDEX `nome_UNIQUE` (`nome` ASC) )
ENGINE = InnoDB;

-- -----------------------------------------------------
-- Table `dojify_1`.`usuario`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`usuario` (
  `id_usuario` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da pessoa/usuário.',
  `id_academia` INT(11) NULL COMMENT 'Vínculo com a academia do usuário.\n',
  `perfil_id` INT NOT NULL COMMENT 'Vínculo com a função do usuário.',
  `nome` VARCHAR(100) NOT NULL COMMENT 'Nome completo do usuário.\n',
  `cpf` CHAR(11) NOT NULL COMMENT 'CPF do usuário (somente números).\n',
  `data_nascimento` DATE NOT NULL COMMENT 'Data de nascimento.\n',
  `telefone` VARCHAR(20) NOT NULL COMMENT 'Telefone de contato pessoal.\n',
  `email` VARCHAR(100) NOT NULL COMMENT 'E-mail do usuário para acesso.',
  `especialidade` VARCHAR(100) NULL DEFAULT NULL COMMENT 'Arte marcial/foco de ensino (Específico de Professor).',
  `data_admissao` DATE NULL DEFAULT NULL COMMENT 'Data de contratação (Específico de Professor/Funcionário).\n',
  `responsavel` VARCHAR(100) NULL DEFAULT NULL COMMENT 'Responsável legal (Específico de Aluno menor).\n',
  `observacao` TEXT NULL DEFAULT NULL COMMENT 'Observações médicas ou gerais (Específico de Aluno).\n',
  `data_matricula` DATE NULL DEFAULT NULL COMMENT 'Data de início das atividades (Específico de Aluno).',
  `status` VARCHAR(20) NOT NULL DEFAULT 'ATIVO' COMMENT 'Estado do usuário (ATIVO, INATIVO, SUSPENSO).\n',
  PRIMARY KEY (`id_usuario`),
  UNIQUE INDEX `cpf` (`cpf` ASC) ,
  UNIQUE INDEX `email` (`email` ASC) ,
  INDEX `id_academia` (`id_academia` ASC) ,
  INDEX `idx_status` (`status` ASC) ,
  INDEX `fk_usuario_perfil1_idx` (`perfil_id` ASC) ,
  CONSTRAINT `usuario_ibfk_1`
    FOREIGN KEY (`id_academia`)
    REFERENCES `dojify_1`.`academia` (`id_academia`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE,
  CONSTRAINT `fk_usuario_perfil1`
    FOREIGN KEY (`perfil_id`)
    REFERENCES `dojify_1`.`perfil` (`id_perfil`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`modalidade`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`modalidade` (
  `id_modalidade` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da modalidade.\n',
  `id_academia` INT(11) NOT NULL COMMENT 'Vínculo com a academia proprietária.',
  `nome` VARCHAR(100) NOT NULL COMMENT 'Nome da modalidade (ex: Jiu-Jitsu, Muay Thai).\n',
  `descricao` TEXT NULL DEFAULT NULL COMMENT 'Detalhes sobre a prática ou requisitos.\n',
  PRIMARY KEY (`id_modalidade`),
  INDEX `id_academia` (`id_academia` ASC) ,
  CONSTRAINT `modalidade_ibfk_1`
    FOREIGN KEY (`id_academia`)
    REFERENCES `dojify_1`.`academia` (`id_academia`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`turma`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`turma` (
  `id_turma` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da turma.\n',
  `id_usuario_professor` INT(11) NOT NULL COMMENT 'Vículo com o professor responsável pela turma.\n\n\n',
  `id_modalidade` INT(11) NOT NULL COMMENT 'Vínculo com a modalidade oferecida pela academia.\n',
  `nome` VARCHAR(45) NOT NULL COMMENT 'Nome da turma (ex: Turma Adulto - Noite).\n',
  `capacidade` INT(11) NULL DEFAULT NULL COMMENT 'Número máximo de alunos por aula.',
  `nivel` VARCHAR(45) NULL DEFAULT NULL COMMENT 'Grau de exigência/faixa etária da turma (Iniciante, Intermediário, Avançado, Kids).',
  `status` VARCHAR(20) NULL DEFAULT 'ATIVA' COMMENT 'Indica o ciclo de vida operacional daquela turma dentro da academia (ATIVA, INATIVA, LOTADA, CONCLUÍDA).',
  PRIMARY KEY (`id_turma`),
  INDEX `id_usuario_professor` (`id_usuario_professor` ASC) ,
  INDEX `id_modalidade` (`id_modalidade` ASC) ,
  CONSTRAINT `turma_ibfk_1`
    FOREIGN KEY (`id_usuario_professor`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE,
  CONSTRAINT `turma_ibfk_2`
    FOREIGN KEY (`id_modalidade`)
    REFERENCES `dojify_1`.`modalidade` (`id_modalidade`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`agendamento`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`agendamento` (
  `id_agendamento` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da reserva.\n',
  `id_turma` INT(11) NOT NULL COMMENT 'Turma agendada.\n',
  `id_usuario_aluno` INT(11) NOT NULL COMMENT 'Aluno que solicitou o agendamento.',
  `data_agendamento` DATETIME NOT NULL COMMENT 'Data em que a aula será realizada.',
  `status` VARCHAR(20) NOT NULL DEFAULT 'CONFIRMADO' COMMENT 'Situação da reserva (CONFIRMADO, CANCELADO).\n',
  PRIMARY KEY (`id_agendamento`),
  INDEX `id_turma` (`id_turma` ASC) ,
  INDEX `id_usuario_aluno` (`id_usuario_aluno` ASC) ,
  CONSTRAINT `agendamento_ibfk_1`
    FOREIGN KEY (`id_turma`)
    REFERENCES `dojify_1`.`turma` (`id_turma`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,
  CONSTRAINT `agendamento_ibfk_2`
    FOREIGN KEY (`id_usuario_aluno`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`avaliacao`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`avaliacao` (
  `id_avaliacao` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da avaliação.\n',
  `id_usuario_professor` INT(11) NOT NULL COMMENT 'Professor que aplicou a avaliação.\n',
  `id_usuario_aluno` INT(11) NOT NULL COMMENT 'Aluno avaliado.\n',
  `data_avaliacao` DATE NOT NULL,
  `habilidades_melhorar` TEXT NOT NULL COMMENT 'Descrição técnica das habilidades e pontos a melhorar.',
  `observacoes` TEXT NULL DEFAULT NULL COMMENT 'Registro do professor sobre a evolução técnica, física e comportamental do aluno.',
  PRIMARY KEY (`id_avaliacao`),
  INDEX `id_usuario_professor` (`id_usuario_professor` ASC) ,
  INDEX `id_usuario_aluno` (`id_usuario_aluno` ASC) ,
  CONSTRAINT `avaliacao_ibfk_1`
    FOREIGN KEY (`id_usuario_professor`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE,
  CONSTRAINT `avaliacao_ibfk_2`
    FOREIGN KEY (`id_usuario_aluno`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`graduacao`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`graduacao` (
  `id_graduacao` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único do registro de graduação.',
  `id_usuario_aluno` INT(11) NOT NULL COMMENT 'Aluno que possui a graduação.\n',
  `id_usuario_professor` INT(11) NOT NULL COMMENT 'Professor que concedeu a graduação.',
  `id_modalidade` INT(11) NOT NULL COMMENT 'Modalidade correspondente.\n',
  `data_graduacao` DATE NOT NULL COMMENT 'Data da concessão da graduação.\n',
  `faixa` VARCHAR(50) NOT NULL COMMENT 'Cor da faixa concedida.\n',
  `grau` VARCHAR(20) NULL DEFAULT NULL COMMENT 'Número de graus concedidos na faixa.',
  PRIMARY KEY (`id_graduacao`),
  INDEX `id_usuario_aluno` (`id_usuario_aluno` ASC) ,
  INDEX `id_usuario_professor` (`id_usuario_professor` ASC) ,
  INDEX `id_modalidade` (`id_modalidade` ASC) ,
  CONSTRAINT `graduacao_ibfk_1`
    FOREIGN KEY (`id_usuario_aluno`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE,
  CONSTRAINT `graduacao_ibfk_2`
    FOREIGN KEY (`id_usuario_professor`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE,
  CONSTRAINT `graduacao_ibfk_3`
    FOREIGN KEY (`id_modalidade`)
    REFERENCES `dojify_1`.`modalidade` (`id_modalidade`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`horario_turma`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`horario_turma` (
  `id_horario` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único do horário.',
  `id_turma` INT(11) NOT NULL COMMENT 'Vínculo com a turma associada ao horário.\n',
  `dia_semana` VARCHAR(20) NOT NULL COMMENT 'Dia da semana em que a aula ocorre.\n',
  `hora_inicio` TIME NOT NULL COMMENT 'Horário de início da aula.',
  `hora_fim` TIME NOT NULL COMMENT 'Horário de término da aula.\n',
  PRIMARY KEY (`id_horario`),
  INDEX `id_turma` (`id_turma` ASC) ,
  CONSTRAINT `horario_turma_ibfk_1`
    FOREIGN KEY (`id_turma`)
    REFERENCES `dojify_1`.`turma` (`id_turma`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`login`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`login` (
  `id_login` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da credencial.\n',
  `id_usuario` INT(11) NOT NULL COMMENT 'Vínculo diretocom a tabela usuario.',
  `senha_hash` VARCHAR(255) NOT NULL COMMENT 'Hash da senha de acesso do usuário.\n',
  `token_recuperacao` VARCHAR(45) NULL COMMENT 'Token temporário para redefinição de senha.',
  `ultimo_login` DATETIME NULL DEFAULT NULL COMMENT 'Data e hora da última autenticação.',
  PRIMARY KEY (`id_login`),
  UNIQUE INDEX `id_usuario` (`id_usuario` ASC) ,
  CONSTRAINT `login_ibfk_1`
    FOREIGN KEY (`id_usuario`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`plano`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`plano` (
  `id_plano` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único do plano.',
  `id_usuario_aluno` INT(11) NOT NULL COMMENT 'Aluno titular do plano.\n',
  `nome_plano` VARCHAR(100) NOT NULL COMMENT 'Nome do plano (2x/semana, 3x/semana, 5x/semanal).',
  `valor` DECIMAL(10,2) NOT NULL COMMENT 'Valor recorrente cobrado pelo plano.',
  `data_inicio` DATE NOT NULL COMMENT 'Data de início de vigência do plano.',
  `data_fim` DATE NULL COMMENT 'Data do fim da vigência do plano.\n',
  `status` VARCHAR(20) NOT NULL DEFAULT 'ATIVO' COMMENT 'Estado do plano (ATIVO, CANCELADO, SUSPENSO).',
  PRIMARY KEY (`id_plano`),
  INDEX `id_usuario_aluno` (`id_usuario_aluno` ASC) ,
  CONSTRAINT `plano_matricula_ibfk_1`
    FOREIGN KEY (`id_usuario_aluno`)
    REFERENCES `dojify_1`.`usuario` (`id_usuario`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`pagamento`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`pagamento` (
  `id_pagamento` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único do registro de cobrança.',
  `id_plano_matricula` INT(11) NOT NULL COMMENT 'Plano que originou a fatura.',
  `valor` DECIMAL(10,2) NOT NULL COMMENT 'Quantia recebida na liquidação.',
  `data_vencimento` DATE NOT NULL COMMENT 'Data de vencimento do título.',
  `data_pagamento` DATE NULL DEFAULT NULL COMMENT 'Data em que a quitação foi efetuada.\n',
  `forma_pagamento` VARCHAR(50) NULL DEFAULT NULL COMMENT 'Método usado (PIX, CARTAO, DINHEIRO).',
  `status` VARCHAR(20) NULL DEFAULT 'PENDENTE' COMMENT 'Estado do pagamento (PENDENTE, PAGO, ATRASADO).',
  `observacao` TEXT NULL DEFAULT NULL,
  PRIMARY KEY (`id_pagamento`),
  INDEX `id_plano_matricula` (`id_plano_matricula` ASC) ,
  CONSTRAINT `pagamento_ibfk_1`
    FOREIGN KEY (`id_plano_matricula`)
    REFERENCES `dojify_1`.`plano` (`id_plano`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;

-- -----------------------------------------------------
-- Table `dojify_1`.`presenca`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `dojify_1`.`presenca` (
  `id_presenca` INT(11) NOT NULL AUTO_INCREMENT COMMENT 'Identificador único da presença.',
  `id_agendamento` INT(11) NOT NULL COMMENT 'Agendamento correspondente.',
  `data` DATE NOT NULL COMMENT 'Data de realização da aula.',
  `status` TINYINT(1) NOT NULL COMMENT 'Indicador de presença (1 = Presente, 0 = Ausente).',
  PRIMARY KEY (`id_presenca`),
  INDEX `id_agendamento` (`id_agendamento` ASC) ,
  CONSTRAINT `presenca_ibfk_1`
    FOREIGN KEY (`id_agendamento`)
    REFERENCES `dojify_1`.`agendamento` (`id_agendamento`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;




-- INSERTS PARA TESTE!


USE `dojify_1`;

-- ==========================================================
-- 1. PERFIL
-- ==========================================================
INSERT INTO perfil (id_perfil, nome) VALUES 
(1, 'ADMINISTRADOR'),
(2, 'GERENTE'),
(3, 'PROFESSOR'),
(4, 'ALUNO');

-- ==========================================================
-- 2. ACADEMIA
-- ==========================================================
INSERT INTO academia (id_academia, nome, documento, endereco, telefone, email) VALUES 
(1, 'A TOKKA - Escola de Lutas', '66790246000112', 'Qnm 08 Conj. B lote 34 - Ceilândia, Brasília - DF', '61998693504', 'contato@atokka.com');

-- ==========================================================
-- 3. USUARIOS (Admin, Gerente, Professor e Alunos)
-- ==========================================================
-- Admin
INSERT INTO usuario (id_usuario, id_academia, perfil_id, nome, cpf, data_nascimento, telefone, email, status) VALUES 
(1, NULL, 1, 'Admin(Suporte)', '00000000000', '1990-01-01', '11900000000', 'admin@dojify.com', 'ATIVO');

-- Gerente
INSERT INTO usuario (id_usuario, id_academia, perfil_id, nome, cpf, data_nascimento, telefone, email, status) VALUES 
(2, 1, 2, 'Carlos Silva (Gerente)', '11111111111', '1985-04-12', '11988887777', 'gerentecarlos@dojify.com', 'ATIVO');

-- Professor
INSERT INTO usuario (id_usuario, id_academia, perfil_id, nome, cpf, data_nascimento, telefone, email, especialidade, data_admissao, status) VALUES 
(3, 1, 3, 'Mestre Ratto', '22222222222', '1978-08-20', '11977776666', 'ratto@dojify.com', 'Jiu-Jitsu', '2020-01-15', 'ATIVO');

-- Alunos
INSERT INTO usuario (id_usuario, id_academia, perfil_id, nome, cpf, data_nascimento, telefone, email, data_matricula, status) VALUES 
(4, 1, 4, 'João Souza', '33333333333', '2002-05-10', '11966665555', 'joao@email.com', '2026-01-10', 'ATIVO'),
(5, 1, 4, 'Sebastiana Oliveira', '44444444444', '1998-11-25', '11955554444', 'sebastiana@email.com', '2026-02-01', 'ATIVO');

-- ==========================================================
-- 4. LOGIN (Hash de senhas de teste)
-- ==========================================================
INSERT INTO `login` (`id_login`, `id_usuario`, `senha_hash`) VALUES 
(1, 1, '$2y$10$IavQv0n.9H8Vq9hbRKoZSu6.YYxdRjgzKRNhiG3UuqaX45Mr9q5bu'), -- admin123
(2, 2, '$2y$10$GK6AB4AFeGkIIEv1x5ok2.57q3ZbSp4643MVjvuTCEQeU57Bj5/DC'), -- gerente123
(3, 3, '$2y$10$QeVSOdmtx/rEy7wUI/Ith.jKSWlGjRQMb9Ll44CF/pTPs7/LbNBdG'), -- prof123
(4, 4, '$2y$10$cbDvKrrWibvx41K.46HLQu0d.5V80KHO8tg1aJypmNY0vMRnQbo0C'), -- aluno123
(5, 5, '$2y$10$cbDvKrrWibvx41K.46HLQu0d.5V80KHO8tg1aJypmNY0vMRnQbo0C'); -- aluno123
-- ==========================================================
-- 5. MODALIDADE
-- ==========================================================
INSERT INTO modalidade (id_modalidade, id_academia, nome, descricao) VALUES 
(1, 1, 'Jiu-Jítsu', 'Arte suave focada em luta no chão e finalizações'),
(2, 1, 'Muay Thai', 'Arte com socos, chutes, joelhadas e cotoveladas');

-- ==========================================================
-- 6. TURMA E HORARIO_TURMA
-- ==========================================================
INSERT INTO turma (id_turma, id_usuario_professor, id_modalidade, nome, capacidade, nivel, status) VALUES 
(1, 2, 1, 'Jiu-Jitsu Adulto Noite', 20, 'Intermediário', 'ATIVA');

INSERT INTO horario_turma(id_horario, id_turma, dia_semana, hora_inicio, hora_fim) VALUES 
(1, 1, 'Segunda-Feira', '19:00:00', '20:30:00'),
(2, 1, 'Quarta-Feira', '19:00:00', '20:30:00');

-- ==========================================================
-- 7. PLANO E PAGAMENTO
-- ==========================================================
INSERT INTO plano(id_plano, id_usuario_aluno, nome_plano, valor, data_inicio, data_fim, status) VALUES 
(1, 3, 'Mensal 3x/semana', 140.00, '2026-03-01', '2026-04-01', 'ATIVO'),
(2, 4, 'Mensal 2x/semana', 100.00, '2026-03-01', '2026-06-01', 'ATIVO');

INSERT INTO pagamento(id_pagamento, id_plano_matricula, valor, data_vencimento, data_pagamento, forma_pagamento, status) VALUES 
(1, 1, 140.00, '2026-03-10', '2026-03-08', 'PIX', 'PAGO'),
(2, 2, 100.00, '2026-03-10', NULL, NULL, 'PENDENTE');

-- ==========================================================
-- 8. AGENDAMENTO, PRESENCA E GRADUACAO
-- ==========================================================
INSERT INTO agendamento(id_agendamento, id_turma, id_usuario_aluno, data_agendamento, status) VALUES 
(1, 1, 3, '2026-03-16 19:00:00', 'CONFIRMADO');

INSERT INTO presenca(id_presenca, id_agendamento, data, status) VALUES 
(1, 1, '2026-03-16', 1);

INSERT INTO graduacao(id_graduacao, id_usuario_aluno, id_usuario_professor, id_modalidade, data_graduacao, faixa, grau) VALUES 
(1, 3, 2, 1, '2026-01-15', 'Branca', '2º Grau');



